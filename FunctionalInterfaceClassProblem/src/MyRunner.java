public class MyRunner {
   public static void main(String[] args) {
       MainEntry compareObj = new MainEntry();
       int num[] = {2,2,4,4};
       compareObj.add.compute(num);

       compareObj.sub.compute(num);
       compareObj.multiply.compute(num);
       compareObj.div.compute(num);
       
   }
}
